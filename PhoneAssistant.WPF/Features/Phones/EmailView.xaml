<UserControl x:Class="PhoneAssistant.WPF.Features.Phones.EmailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PhoneAssistant.WPF.Features.Phones"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=local:PhonesMainViewModel,
                                              IsDesignTimeCreatable=False}"
             d:DesignHeight="200"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel Margin="16" Orientation="Horizontal">

        <TextBlock Margin="4" Text="Phone" />

        <TextBlock Margin="0,0,5,0"
                   d:Background="AntiqueWhite"
                   d:Text="IMEI"
                   Text="{Binding Imei}" />
        <TextBlock Margin="4" Text="PhoneNumber" />

        <TextBlock Margin="0,0,5,0"
                   d:Background="AntiqueWhite"
                   d:Text="PhoneNumber"
                   Text="{Binding PhoneNumber}" />

        <RichTextBox Height="80"
                     MinWidth="280"
                     materialDesign:HintAssist.Hint="Multiline text"
                     AcceptsReturn="True"
                     IsDocumentEnabled="True"
                     SpellCheck.IsEnabled="True"
                     VerticalScrollBarVisibility="Auto">
            <!--<FlowDocument>
                <Paragraph>
                    <Run FontWeight="Bold">Rich Text Box.</Run>
                    <LineBreak />
                    <Run FontStyle="Italic">With formatting support</Run>
                    <LineBreak />
                    <Hyperlink Cursor="Hand" NavigateUri="https://github.com/MaterialDesignInXAML/MaterialDesignInXamlToolkit">
                        Material Design in XAML
                    </Hyperlink>
                </Paragraph>
            </FlowDocument>-->
        </RichTextBox>

        <Button Margin="4"
                Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
                CommandParameter="{x:Static Dock.Top}"
                Content="CLOSE THIS"
                Style="{StaticResource MaterialDesignFlatButton}" />
    </StackPanel>

</UserControl>
