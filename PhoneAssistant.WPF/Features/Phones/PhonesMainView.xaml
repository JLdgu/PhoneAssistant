<UserControl
    x:Class="PhoneAssistant.WPF.Features.Phones.PhonesMainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shared="clr-namespace:PhoneAssistant.WPF.Shared"
    xmlns:viewmodel="clr-namespace:PhoneAssistant.WPF.Features.Phones"
    d:DataContext="{d:DesignInstance Type=viewmodel:PhonesMainViewModel}"
    d:DesignHeight="200"
    d:DesignWidth="1000"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <shared:MainViewHeader HeaderIcon="CellphoneScreenshot" HeaderText="Phones" />
        <Grid
            Grid.Row="1"
            Margin="16,0,0,5"
            d:Background="LightCoral">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Margin="0,0,5,0"
                materialDesign:HintAssist.Hint="IMEI"
                Text="{Binding SelectedPhone.IMEI, Mode=TwoWay}" />
            <TextBox
                Grid.Column="3"
                Margin="0,0,5,0"
                materialDesign:HintAssist.Hint="Former User"
                Text="{Binding SelectedPhone.FormerUser, Mode=TwoWay}" />
        </Grid>

        <DataGrid
            Name="dataGrid1"
            Grid.Row="2"
            AlternatingRowBackground="{DynamicResource PrimaryHueLightBrush}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserReorderColumns="False"
            CanUserSortColumns="True"
            IsReadOnly="True"
            ItemsSource="{Binding Phones}"
            SelectedItem="{Binding SelectedPhone}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding IMEI}" Header="IMEI" />
                <DataGridTextColumn Binding="{Binding FormerUser}" Header="Former User" />
                <DataGridCheckBoxColumn Binding="{Binding Wiped}" Header="Wiped" />
                <DataGridTextColumn Binding="{Binding Status}" Header="Status" />
                <DataGridTextColumn Binding="{Binding OEM}" Header="OEM" />
                <DataGridTextColumn Binding="{Binding AssetTag}" Header="Asset Tag" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>