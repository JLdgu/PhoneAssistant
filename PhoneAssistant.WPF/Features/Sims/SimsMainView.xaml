<UserControl x:Class="PhoneAssistant.WPF.Features.Sims.SimsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodel="clr-namespace:PhoneAssistant.WPF.Features.Sims" 
             xmlns:shared="clr-namespace:PhoneAssistant.WPF.Shared"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodel:}"
             d:DesignHeight="250" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="SimCardItemTemplate">
            <StackPanel Orientation="Horizontal" d:Background="LightCoral">
                <TextBlock Text="{Binding SelectedSimCard.PhoneNumber}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <shared:MainViewHeader HeaderText="SIM Cards" 
                               HeaderIcon="SimOutline"/>

        <ListView Grid.Row="2" Margin="0" d:Height="40"
                  SelectionMode="Single"
                  ItemsSource="{Binding Sims}"
                  SelectedItem="{Binding SelectedSimCard}">
            <ListView.View>
                <GridView AllowsColumnReorder="False">

                    <GridViewColumn DisplayMemberBinding="{Binding Path=PhoneNumber}" Header="Phone Number" Width="auto"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=SimNumber}" Header="SIM Card NUmber" Width="auto"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Status}" Header="Status" Width="auto"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Note}" Header="Note" Width="300"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
